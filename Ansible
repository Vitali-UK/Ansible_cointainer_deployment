---
- name: Blazor app production container 
  hosts: localhost
  connection: local
  gather_facts: no
  vars:
    container_name: Blazor production
    image: nginx:latest
  tasks:
  - name: Run container 
    docker_container:
      name: "{{ container_name }}"
      image: "{{ image }}"
      state: started
